# Neon Postgres connection string used to save order requests submitted
# through the website's order form (src/app/api/orders/route.ts).
# Get this from the Neon console (project: "Color Furniture") and set it
# as an environment variable on your hosting platform (Netlify, Vercel, etc.)
# — never commit the real value.
DATABASE_URL=
